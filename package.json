{
  "name": "looking-for-group",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "license": "CC0-1.0",
  "workspaces": [
    "client",
    "server",
    "database"
  ],
  "scripts": {
    "prepare": "husky && npm run prisma:generate",
    "dev": "concurrently \"npm run dev --workspace=client\" \"npm run dev --workspace=server\"",
    "prisma:pull": "npx --workspace=server prisma db pull",
    "prisma:generate": "npx --workspace=server prisma generate",
    "build:database": "npm run build:image --workspace=database",
    "build:client": "npm run build --workspace=client",
    "test": "npm run test:front",
    "test:front": "vitest run --config server/src/config/vitest.ts",
    "test:back": "cross-env NODE_ENV=test jest",
    "lint:server:types": "npm run lint:types --workspace=server",
    "lint:server": "npm run lint --workspace=server",
    "lint:client:types": "npm run lint:types --workspace=client",
    "lint:client": "npm run lint --workspace=client",
    "format:server": "npm run format --workspace=server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "cross-env": "^7.0.3",
    "husky": "^9.1.7",
    "jest": "^29.7.0",
    "jsdom": "^26.1.0",
    "lint-staged": "^16.1.0",
    "nodemon": "^3.1.9",
    "prettier": "^3.5.3",
    "supertest": "^7.1.1",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=v23.6.0"
  }
}
